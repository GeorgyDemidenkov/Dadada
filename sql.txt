-- Создание базы данных (если еще не создана)
CREATE DATABASE IF NOT EXISTS KioskDB;

USE KioskDB;

-- Таблица продуктов с минимальным уровнем запаса
CREATE TABLE IF NOT EXISTS products (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    category ENUM('Газета', 'Журнал', 'Книга') NOT NULL,
    quantity INT NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    minimal_level INT DEFAULT 5
);

-- Таблица заказов
CREATE TABLE IF NOT EXISTS orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_id INT NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    quantity_ordered INT NOT NULL,
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status ENUM('PENDING', 'COMPLETED') DEFAULT 'PENDING',
    FOREIGN KEY (product_id) REFERENCES products(id)
);

-- Вставка тестовых данных (опционально)
INSERT INTO products (name, category, quantity, price, minimal_level) VALUES
('Газета Утро', 'Газета', 20, 50.00, 5),
('Журнал Мода', 'Журнал', 15, 150.00, 3),
('Книга Программирование', 'Книга', 8, 500.00, 2);
